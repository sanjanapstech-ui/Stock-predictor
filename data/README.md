# Data folder

- `sample_template.csv`: small synthetic example showing the minimum required columns (`Date`, `Close`).

For real forecasts, upload a CSV with real historical prices (for example, exported from Yahoo Finance).

